name: Build With JDK25

on:
  push:
    branches: [ "26.1", "neoforge-26.1", "26.2", "neoforge-26.2"  ]
  pull_request:
    branches: [ "26.1", "neoforge-26.1", "26.2", "neoforge-26.2"  ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build and test with Gradle
        run: ./gradlew build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-jar
          path: build/libs/*.jar